# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# Invoices
GET   /invoice              controllers.Application.invoices
GET   /invoice/:id          controllers.Application.invoice(id)
GET   /invoice/:id/item     controllers.Application.invoiceItems(id)
POST  /invoice              controllers.Application.addInvoice
POST  /invoice/:id/item     controllers.Application.addInvoiceItem(id)
